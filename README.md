# Edit videos from terminal

## Trim script
Move the trim.sh file to the directory of the video and run it.
Enter the timestamps you need and rest of the video will remove (more options inside the script) 
